services:
  postgres:
    image: postgres:17-alpine
    container_name: ifc-fiscaliza-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ifc_fiscaliza
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
